{{#each this}}
  <li class='glide__slide footer-modal__card'>

    <img class='student-foto' src={{img}} alt={{name}} />
    <div class='student__card'>
      <h3 class='student__name'>{{name}}</h3>
      <p class='student__role'>{{role}}</p>
      <p class='student__about'>
        {{discription}}
      </p>
    </div>
  </li>

{{/each}}